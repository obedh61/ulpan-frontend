# URL del backend API (Render)
# En desarrollo no se necesita — Vite proxy redirige /api a localhost:5000
# En producción (Vercel): poner la URL completa del backend
VITE_API_URL=https://ulpan-backend.onrender.com/api
